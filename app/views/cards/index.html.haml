%h1 Cards

%table.table.table-dark
  %thead
    %tr
      %th Title
      %th Earliest start
      %th Length
      %th Task type
      %th Dependent on
      %th Description
      %th Assigned to
      %th
      %th
      %th

  %tbody
    - @cards.each do |card|
      %tr
        %td= card.title
        %td= card.earliest_start
        %td= card.length
        %td= card.task_type
        %td= card.dependent_on
        %td= card.description
        %td= card.assigned_to
        %td= link_to 'Show', card
        %td= link_to 'Edit', edit_card_path(card)
        %td= link_to 'Destroy', card, method: :delete, data: { confirm: 'Are you sure?' }
