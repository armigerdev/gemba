%h1 Lists

%table.table.table-dark
  %thead
    %tr
      %th Belongs To
      %th Title
      %th Description
      %th Cards
      %th
      %th
      %th

  %tbody
    - @lists.each do |list|
      %tr
        %td= Board.find(list.board_id).title
        %td= list.title
        %td= list.description
        %td= Card.where(list_id: list.id).count
        %td= link_to 'Show', list
        %td= link_to 'Edit', edit_list_path(list)
        %td= link_to 'Destroy', list, method: :delete, data: { confirm: 'Are you sure?' }
